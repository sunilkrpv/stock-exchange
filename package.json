{
  "name": "trade-xchange",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "rimraf dist",
    "lint": "eslint",
    "build": "tsc --project ./",
    "start": "node dist/main.js",
    "debug": "cd dist && node server.js"
  },
  "author": "sunil",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.14.10",
    "rimraf": "^6.0.1"
  },
  "dependencies": {
    "@types/collections": "^5.1.5",
    "csv-parser": "^3.0.0",
    "data-structure-typed": "^1.52.0"
  }
}
